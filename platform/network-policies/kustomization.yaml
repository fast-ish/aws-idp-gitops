apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# =============================================================================
# Platform Network Policies
# Enforces least-privilege network segmentation for platform components
# =============================================================================

resources:
  - observability-netpol.yaml
  - argocd-netpol.yaml
  - argo-workflows-netpol.yaml
  - kyverno-netpol.yaml
  - external-secrets-netpol.yaml
  - falco-netpol.yaml

commonLabels:
  app.kubernetes.io/part-of: platform-security
  app.kubernetes.io/component: network-policy
